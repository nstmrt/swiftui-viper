//
//  {{ prefix }}{{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import SMUIKit

typealias {{ module_info.name }}Module = (view: Presenting, input: {{ module_info.name }}Input)

final class {{ module_info.name }}Configurator {
    func configure(router: {{ module_info.name }}Output) -> {{ module_info.name }}Module {
        let viewState = {{ module_info.name }}ViewState()

        let interactor = {{ module_info.name }}Interactor()

        let presenter = {{ module_info.name }}Presenter(interactor: interactor,
                                                        viewState: viewState,
                                                        router: router)

        let view = {{ module_info.name }}View(output: presenter, state: viewState)

        let wrappedView = CustomHostingController(rootView: AnyView(view))
        wrappedView.delegate = presenter

        return (wrappedView, presenter)
    }
}
